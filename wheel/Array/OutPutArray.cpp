/* 方法名：outputdata 
 * 作用：for each 循环输出所有数组中的数 
 * 参数：数组，输入数值 
 * 返回值：void 
 */
void outputdata(int a[],int n)
{
	for(int i = 0; i < n ; i++)
	{
		printf("%d ", a[i]); 	
	}                          
		  
} 